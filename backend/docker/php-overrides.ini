; Runtime limits for the solar-proposal backend. Four sequential Gemini
; image calls per render can return large base64 payloads, and the whole
; flow plus GD compositing is memory-heavy, so raise the defaults.

memory_limit = 1024M
max_execution_time = 300
post_max_size = 64M
upload_max_filesize = 64M
