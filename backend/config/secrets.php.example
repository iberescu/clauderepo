<?php

/*
|--------------------------------------------------------------------------
| Runtime credentials template
|--------------------------------------------------------------------------
|
| Copy this file to config/secrets.php and fill in real values to run
| against the live Google + Gemini APIs. config/solar.php reads whichever
| of the two exists, so the app always boots.
|
| Flip `fake_providers` to false once the three keys are set.
|
*/

return [
    'fake_providers'        => true,

    'google_maps_api_key'   => '',
    'google_solar_api_key'  => '',
    'gemini_api_key'        => '',
];
